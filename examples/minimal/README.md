# Minimal example

This folder is intentionally small: it is a placeholder for a minimal reproducible case.

Recommended contents:
- a tiny `.k` file (or template)
- how to run it with `python -m lsdyna_py.runner.cli ...`
- expected postprocess outputs (CSV) but **not** `d3plot/binout` files
